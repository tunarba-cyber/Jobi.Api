namespace LinkedIn.Modules.Jobs.Infrastructure.Persistence;

/// <summary>Schema owned by the Jobs module. One database, isolated schemas per module.</summary>
public static class JobsSchema
{
    public const string Name = "jobs";
}
